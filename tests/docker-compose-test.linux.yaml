version: "3"

services:
  redis:
    image: redis:latest
    ports:
      - "6380:6379"
    tmpfs:
      - /data
  keydb:
    image: bitnami/keydb:latest
    environment:
      - ALLOW_EMPTY_PASSWORD=yes
    ports:
      - "6381:6379"
    tmpfs:
      - /bitnami/keydb/data
